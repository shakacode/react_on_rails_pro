# frozen_string_literal: true

require_relative "../spec_helper"

describe ReactOnRailsPro::Cache do
  describe ".fetch_react_component" do
    it "fetches the value from the cache"
  end
  describe ".base_cache_key" do
    it "has the basic values"

    it "has the bundle_hash if prerender is true"
  end

  describe ".react_component_cache_key" do
    it "properly expands cache keys"
  end
end
